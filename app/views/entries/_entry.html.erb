<li id="entry-<%= entry.id %>">
  <span class="content">
    <%= entry.content %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(entry.created_at) %> ago.
    <%= link_to "View", entry %>
    <%= link_to_if entry.user_id == current_user.id, "Edit", edit_entry_path(entry) %>
  </span>
</li>